spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ecommerce_test
    username: testuser
    password: testpass
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080
